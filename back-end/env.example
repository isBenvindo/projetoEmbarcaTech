# =====================================================================
# Terelina Backend System - Example Environment Configuration
# =====================================================================
#
# Copy this file to the project's root directory and rename it to .env
#
# =====================================================================

# --- Database Connection ---
DB_HOST=db
DB_NAME=terelina_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# --- MQTT Broker Connection ---
MQTT_BROKER_HOST=broker.hivemq.com
MQTT_BROKER_PORT=1883
MQTT_TOPIC_STATE=sensors/barrier/state
MQTT_USERNAME=
MQTT_PASSWORD=

# !!! IMPORTANT !!!
# Each instance of this system (production, development, etc.)
# MUST have a unique MQTT Client ID.
# For production, use something like "terelina_backend_prod_loja01".
# For development, use something like "terelina_backend_dev_yourname".
MQTT_CLIENT_ID=terelina_backend_CHANGE_THIS_TO_UNIQUE_ID

# --- Application Server ---
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO
RELOAD=false